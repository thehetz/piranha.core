@model Piranha.Extend.Fields.StringField
@{
    var placeholder = ViewData["Placeholder"];
    var focus = ViewData["Focus"] != null ? (bool)ViewData["Focus"] : false;
}

@Html.TextBoxFor(m => m.Value, new { @class = "form-control small " + (focus ? "autofocus" : ""), placeholder = placeholder })